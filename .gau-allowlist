# github-auto-upload 密钥扫描白名单
#
# 本文件让「上传前的密钥扫描」跳过下列路径。
# 每行一个 glob，可匹配仓库相对路径或文件名。
#
# 已人工逐条核对：命中的都是「文件名规则」误报或环境变量占位符，
# 文件内不含任何真实凭据。行级误报请优先用行内 `gau:allow` 注释，
# 只有文件名规则（本文件无法定位到具体行）才在这里放行。

# 环境变量模板：所有值均为空占位，供使用者复制为 .env 后填写。
# （真实 .env 已被 .gitignore 排除，永不入库。）
.env.example
